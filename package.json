{
	"name": "nox",
	"version": "1.0.0",
	"description": "NOX - A real-time chat Application",
	"engines": {
		"node": ">=20.0.0"
	},
	"homepage": "https://github.com/its-as2002/NOX#readme",
	"bugs": {
		"url": "https://github.com/its-as2002/NOX/issues"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/its-as2002/NOX.git"
	},
	"license": "ISC",
	"author": "AYUSH SRIVASTAVA",
	"type": "commonjs",
	"main": "index.js",
	"scripts": {
		"build": "npm install --prefix backend && npm install --prefix client && npm run build --prefix client/",
		"start": "npm run start --prefix backend",
		"dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix client\"",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"devDependencies": {
		"concurrently": "^9.2.1"
	}
}
